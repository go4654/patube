extends layouts/main

block content 
    .form-container 
        form(action=`/users${routes.changePassword}` method="post") 
            input(type="password", name="oldPassword", placeholder="현재 비번")
            input(type="password" name="newPassword" placeholder="새로운비번")
            input(type="password" name="newPassword1" placeholder="비번 확인")
            input(type="submit" value="비번 변경")